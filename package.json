{
  "name": "private-nodelink",
  "version": "1.0.0",
  "description": "Native NodeLink on Render Free Tier",
  "main": "dist/index.js",
  "scripts": {
    "install-nodelink": "git clone https://github.com temp_nodelink && cp -r temp_nodelink/* . && cp -r temp_nodelink/.* . && rm -rf temp_nodelink",
    "build": "npm run install-nodelink && npm install --ignore-scripts && npm run build",
    "start": "node dist/index.js"
  }
}
